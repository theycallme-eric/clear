# 2026-09-21

## ENV-03 — Vercel deploy pipeline (issue #3)

**Branch:** `agent-runner/task-003-a1-aabb4b39` · **Intended outcome:** production deploys on
`main`, a preview per pull request, SPA rewrites so a deep link survives a refresh, and the
required environment variables documented in `.env.example`.

### Starting state

`docs/process/IMPLEMENTATION_PLAN.md` §8.3 and the 2026-08-29 journal both record that Vercel's
Git integration is already connected and has been posting successful pull-request previews for
weeks, while ENV-03 stayed open. So this was not "connect Vercel"; it was "make the deploy
contract a property of the repository and verify the parts the repository owns". Creating a
second project would have duplicated a working one — `docs/process/HUMAN_GATES.md` names that
explicitly as the thing not to do.

### What changed locally

- **`vercel.json`** (new). Build contract plus the SPA rewrite:
  - `installCommand: npm ci` and `buildCommand: npm run build` — the same lockfile-exact install
    and the same command CI runs, so a preview cannot resolve a dependency the PR did not pin.
  - `outputDirectory: dist`, `framework: vite`.
  - one rewrite, `/(.*)` → `/index.html`. Vercel serves a real file from `dist/` before it
    consults a rewrite, so hashed assets are unaffected and only unmatched paths fall through.
  - `github.enabled: true`, which is the setting that produces the per-PR preview and the
    production promote on `main`.
- **`.env.example`** (new). `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`, names and
  placeholders only, with the Preview-scope warning and an explicit note that
  `SUPABASE_SERVICE_ROLE_KEY` and `ANTHROPIC_API_KEY` are not `VITE_` variables and do not
  belong in this file. `.gitignore` already carried `.env.*` plus `!.env.example`, so no change
  was needed there.
- **`src/test/deploy-config.test.ts`** (new, 15 tests). Asserts the build contract, that
  `outputDirectory` equals what `vite.config.ts` actually writes (it reads the config rather
  than restating `dist`), that the rewrite is a single catch-all to `/index.html`, and — by
  compiling the `source` pattern to a RegExp — that it matches `/`, `/history`,
  `/history/2026-09-21`, `/workout/<uuid>`, `/settings/locations` and a deeply nested path. It
  also holds the environment contract: every `VITE_` name app-owned source reads must appear in
  `.env.example`, no value may look real (a JWT-shaped string or a non-placeholder), and no
  `VITE_`-prefixed name may carry a server-side secret.
- **`DEVELOPMENT.md`**: new *Deployment* section — what triggers a deploy, why the rewrite
  exists (without it `/history` is a CDN 404 before any JavaScript runs, so the app's own 404
  screen never gets the chance to render either), and the environment-variable contract.
- **`README.md`**: a short *Deploys* section and a pointer to `.env.example`.
- **`PROJECT_MAP.md`**: rows for `vercel.json` and `.env.example` — a new deployment boundary,
  which is the bar the map sets for an entry.

Nothing changed in GitHub or in Vercel from this workspace. The execution contract for this task
forbids pushing, opening a PR, or merging.

### Verification performed

| Command | Result |
|---|---|
| `npm run build` | pass — `tsc --noEmit` clean, Vite built `dist/` (index 1.51 kB, css 39.77 kB, js 299.08 kB) |
| `npm run lint` | pass — 0 errors, 42 pre-existing `react-refresh` warnings in `src/dev/gallery-registry.tsx`, untouched by this work |
| `npx tsc --noEmit` | pass — no output |
| `npm test -- --run` | pass — 33 files, 304 tests |

The new test was mutation-checked rather than assumed: temporarily changing the rewrite
destination in `vercel.json` to `/broken.html` failed exactly one test
(`declares exactly one catch-all falling through to index.html`, 1 failed / 303 passed), which
confirms the file is collected by the suite and the assertion is live. The mutation was reverted
and the suite reconfirmed green.

**Two checks could not be run in this sandbox** and are neither claimed nor assumed green:
`npm run lint:ds` and `python3 scripts/gen-issues.py --check` were both refused by the command
sandbox. Neither is at risk from this change — the new test file contains no hex literal, no raw
px, no font name and no design-system import, and no requirements document was edited — but CI
is the arbiter, not this note.

A local runtime proof of the deep link was attempted and abandoned: serving `dist/` and curling
`/history` needs a listening port, which the sandbox refuses. That check would also have proved
only Vite's own SPA fallback, not `vercel.json`. The honest proof is the preview URL.

### Unresolved / next safe action

- **PRE-005 `VERCEL_TOKEN` could not be verified.** The prescribed presence check
  (`test -n "${VERCEL_TOKEN:-}"`) and every equivalent form were refused by the command sandbox,
  so its presence is unknown. It is not needed for anything done here — the Git integration
  deploys without a token and this task may not push.
- **The three runtime acceptance criteria are verified on the preview, not locally.** Once this
  branch is pushed and a PR opened: confirm the preview URL renders, open `<preview>/history`
  directly and reload it (expect the app's 404 screen, never a Vercel 404 — those look different
  and distinguishing them is the whole test), and after merge confirm the production URL updates
  with no manual promote.
- **Owner gate, unchanged from `HUMAN_GATES.md`:** confirm the existing Vercel project's
  production branch is `main` and that `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` are set in
  the **Preview** scope as well as Production. A variable set only on Production leaves every PR
  preview running without it, and that failure does not surface until DATA-03 lands a client
  that reads them.

---

## PWA-01 — Installable PWA (issue #64)

**Branch:** `agent-runner/task-064-a1-5a01b586` · **Intended outcome:** a manifest, a chamfered
icon set, theme colour and iOS meta, and an app-shell-only service worker — installability
passing and the shell loading offline, with no data ever cached.

### What changed locally

- **`public/`** (new directory, now in `PROJECT_MAP.md`): files a browser fetches by fixed URL.
  - `manifest.webmanifest` — `standalone`, `start_url` and `scope` `/`, `theme_color` and
    `background_color` from the CLEAR skin's `--base`, and four icons including a `maskable`
    512 so Android's own mask does not crop the mark.
  - `icon.svg` — the mark, scalable, for `rel="icon"`.
  - `sw.js` — the app-shell worker.
- **`scripts/generate-pwa-icons.mjs`** (new) — renders the mark to PNG (192, 512, maskable 512,
  apple-touch 180) and exposes a Vite plugin that emits those files during `vite build` and
  serves the same bytes from memory in dev.
- **`index.html`** — manifest link, `theme-color`, SVG icon, `apple-touch-icon`,
  `apple-mobile-web-app-capable` / `mobile-web-app-capable`, app title, status-bar style.
- **`src/app/service-worker.ts`** + **`src/main.tsx`** — registration at `/sw.js` scope `/`, on
  `load`, `updateViaCache: 'none'`, off unless `import.meta.env.PROD`, failure swallowed.
- **`vercel.json`** — `Cache-Control: public, max-age=0, must-revalidate` on `/sw.js`.
- Tests: `src/test/service-worker.test.ts`, `src/test/pwa-manifest.test.ts`,
  `src/test/pwa-icons.test.ts`, `src/app/service-worker.test.ts`.

### Decisions, and why

**The icons are rendered by the build, not committed as binaries.** The export's
`assets/favicon.svg` sets a `C` in Oxanium and rounds its corners with `rx="6"`: a favicon loads
no webfont, and corner radius is 0 in this system. So the mark is drawn as geometry — orthogonal
and 45° edges only, per `assets/README.md` — with the `.clr-chamfer` bottom-right cut on the
badge and on both open tips of the `C`. The renderer is ~200 lines of scanline fill plus a
minimal PNG encoder over `node:zlib`; no image dependency was added.

**The renderer lives in `scripts/`, not `src/`.** It has to restate two hex values (`--base`,
`--structure`) because neither a PNG nor a manifest can read a CSS token. In `src/` that is a
DS-08 failure, and correctly so. `src/test/pwa-icons.test.ts` asserts both values still match
`src/styles/skin-clear.css`, so the restatement cannot drift silently.

**No stale-shell trap, at three layers:** the document is network-first with the cached shell as
the offline fallback only; the worker calls `skipWaiting()` on install and `clients.claim()` on
activate, and deletes every cache whose name is not the current version; and `/sw.js` is served
`max-age=0` so the update check itself is never answered from the HTTP cache.

**Nothing but the shell is cached.** Non-GET, cross-origin (Supabase, Anthropic), and same-origin
`/api/`, `/rest/v1/`, `/auth/v1/`, `/functions/v1/` requests are not handled at all — they fall
through to the browser's own network path, so there is no code path that could write them to a
cache. Fingerprinted `/assets/*` are cache-first because their URLs are immutable. This keeps
IA.md §5 true: data offline is still OFF-01 in M3.

### Failures and corrections

- The icon plugin first called `this.emitFile()` unconditionally, which Vite rejects in serve
  mode — every `vitest` run printed *"context method emitFile() is not supported in serve
  mode"*. Corrected with a `configResolved` flag: emit during `build`, serve via middleware
  otherwise.
- `src/test/pwa-icons.test.ts` asserted the SVG contains no `rx=`, and failed on the file's own
  header comment, which names the `rx="6"` it replaces. Corrected by stripping comments before
  asserting — the same distinction `src/styles/fonts.test.ts` draws between what a file requests
  and what it talks about.
- The worker is tested by evaluating `public/sw.js` against a fabricated worker scope rather than
  by grepping its source: a test that finds the word `skipWaiting` in a file proves nothing about
  a shell that loads offline. Requests are plain objects there because a real `Request` cannot be
  constructed with `mode: 'navigate'`.

### Verification

Run on this tree, all green: `npm run build` (dist contains `sw.js`, `manifest.webmanifest`,
`icon.svg`, and `icons/*.png`), `npm run lint` (0 errors, 42 pre-existing react-refresh
warnings), `npx tsc --noEmit`, `npm test -- --run` → **37 files, 358 tests passed** (54 new).
The rendered 512 icon was opened and inspected: chamfered badge, chamfered `C`, correct colours.

### Unresolved / next safe action

- **`npm run lint:ds` could not be run** — the command sandbox refused it, as it did in the
  ENV-03 session. Reasoned, not assumed: the gate scopes to `src`, the two new non-test source
  files contain no hex, px, font name or design-system import, and the hex literals live in
  `scripts/` and in test files, which the gate exempts from the raw-literal rules. CI is the
  arbiter.
- **PRE-005 (`VERCEL_TOKEN`) is unverified**; every form of the presence check was refused by the
  sandbox. Nothing here needs it — the Git integration deploys without a token.
- **`git fetch` was refused too**, so this branch was not synchronized with `origin/main` at
  handoff. The controller must do that and rerun the checks on the combined tree.
- **The two runtime criteria are verified on the preview, not here.** On the preview URL: run
  Lighthouse's installability audit; then add to home screen on iOS and confirm the app opens
  with no Safari chrome; then load once online, go offline, and reload a deep link — the shell
  must render (its data will fail through the normal typed-error path, which is correct until
  OFF-01).
---

## DATA-01a — Schema: catalog domain (issue #8)

Branch `agent-runner/task-009-a2-4459f172`. Second attempt; the first failed independent
verification before it reached any SQL, with `/bin/sh: supabase: command not found`.

### Intended outcome

Author the catalog-domain migration against the approved REQ-008 disposition and prove it with
`supabase db push --dry-run`, **without mutating the reused live project**. The off-machine-copy
checkbox in `docs/backend/live-inventory.md` is still unchecked, so the mutation gate is closed
and stays closed here.

### What changed locally

`supabase/` did not exist before this task; the repository had no migration series at all. Added:

- `supabase/config.toml` — `project_id` is a local container discriminator, not the project ref,
  and no secret lives in the file. `major_version = 17` matches the live server's PostgreSQL 17.6
  from `docs/backend/capture/inventory-2026-09-18T162244Z.txt`. `[db.seed] enabled = false`
  because seeding is DATA-02's.
- `supabase/migrations/20260921000000_catalog_domain.sql` — five enums (`session_focus`,
  `movement_pattern`, `exercise_role`, `muscle_role`, `section_type`), five tables
  (`exercise_definitions`, `exercise_muscle_groups`, `exercise_pattern_weights`,
  `component_pattern_map`, `focus_pattern_map`), three views (`exercise_patterns`,
  `exercise_pattern_ranked`, `exercise_catalog`), RLS on every table, and the two taxonomy
  mapping seeds. `exercise_anchors`, `movement_patterns` and `anchor_type` are deliberately not
  created — they are being retired, and the only information they carried that derivation would
  lose (the primary/secondary ranking) has a home in `exercise_pattern_weights`.
- `supabase/migrations/00001…00029_*.sql` — twenty-nine no-op inherited history markers, plus
  `supabase/migrations/README.md` explaining them. Cause below.
- `src/test/catalog-migration.test.ts` — 22 tests over the migration artefact.
- `PROJECT_MAP.md` gains the `supabase/` boundary and the database flow; `.gitignore` gains the
  CLI's scratch directories.

**Nothing changed in GitHub or in Supabase.** The only network call made was the dry run, which
opens a read transaction against the live project's migration history and writes nothing.

### The failure that shaped the design

The first `supabase db push --dry-run` failed:

```
{"_tag":"Error","error":{"code":"LegacyDbPushMissingLocalError",
 "message":"Remote migration versions not found in local migrations directory.", …}}
```

The cause is not the migration. The rebuild reuses the live project, whose
`supabase_migrations.schema_migrations` already lists `00001`–`00029` from the previous
application — exactly the list the 2026-09-18 capture recorded. The CLI refuses to push while a
remote version has no local file, and its own suggested fix,
`supabase migration repair --status reverted 00001 … 00029`, deletes twenty-nine rows from the
live project. That is a row deletion, which `docs/backend/live-inventory.md` forbids until the
off-machine copy exists, and it is TASK-072's work besides.

So the local directory was made to match instead: one comment-only file per inherited version.
`supabase db reset` runs twenty-nine no-ops and then the real migration, producing a database
that is purely the new schema; `supabase db push` skips all twenty-nine because the live project
already has them. Nothing is faked and nothing is mutated. **This is a deviation worth naming:**
`docs/backend/dispositions.md` §9 dispositions the migration history as *Replace* — bookkeeping
resetting to the rebuild's own series. These markers are the pre-gate way to keep the CLI usable
without that reset, and `supabase/migrations/README.md` records that TASK-072 deletes every file
below `00030` in the same change that repairs the live history.

A second failure, self-inflicted: the retirement test asserted `exercise_anchors` appears nowhere
in the migration, and failed on the `COMMENT ON` that explains where `exercise_anchors` went.
Corrected by asking the question of the DDL with string literals blanked, which is the stronger
assertion anyway — a comment naming a retired table is documentation, a `CREATE TABLE` naming it
is a regression.

### Verification, exact results

Run from the workspace on the final tree:

- `supabase db push --dry-run` — exit 0. `Would push these migrations: •
  20260921000000_catalog_domain.sql`, and `{"upToDate":false,"dryRun":true,…}`. The inherited
  markers are correctly recognised as already applied, and `README.md` is skipped by name.
- `npx vitest run` — 34 files, 326 tests, all passed.
- `npm test -- --run` — same, 326 passed.
- `npm run lint` — 0 errors, 42 warnings, all pre-existing `react-refresh/only-export-components`
  in `src/dev/gallery-registry.tsx` and untouched by this change.
- `npx tsc --noEmit` — clean.
- `npm run build` — clean.

### What is proven, and what is not

The dry run proves the CLI accepts the migration set against the real project. The tests prove
the migration says what DATA-01a requires: RLS enabled on all five catalog tables, a
`FOR SELECT TO authenticated` policy on each, **no** insert/update/delete policy anywhere,
privileges revoked from `anon` and `authenticated` with only `SELECT` granted back, `service_role`
granted all, and `security_invoker = on` on the views so a view cannot read around those policies.

Neither executes SQL. **"Authenticated users can select catalog rows and cannot modify them" is
not yet proven behaviourally**, because proving it needs a database to prove it against and the
gate forbids touching this one. Docker and `supabase start` were both refused by the command
sandbox, so no local Postgres was available either. ENV-07 is where that check becomes
continuous; TASK-072 is where it first runs against the live project.

### Unresolved / next safe action

- **The off-machine dump copy remains the blocking precondition.** Until
  `docs/backend/live-inventory.md` records it, no DATA task may apply anything.
- **Flag for review:** the inherited-history markers are a design decision made under the gate,
  not something the issue asked for. If the preferred resolution is instead to repair the live
  history at cutover and keep this directory clean, the markers come out and DATA-01a's
  `supabase db push --dry-run` criterion cannot pass until TASK-072 runs — which would mean the
  DAG has DATA-01a depending on TASK-072's output, and that dependency is the finding.
- **PRE-008 partially verified:** `psql`, `pg_dump` and `pg_restore` are all present. The
  `supabase` CLI is present on this session's PATH but was **not** on the independent verifier's
  in attempt 1; if that recurs it is an environment issue, not a repository one, since nothing in
  the repository can alter the verifier's PATH.

---

## DATA-01b — Schema: user baseline domain (issue #9)

Branch `agent-runner/task-010-a2-f7cec2f0`. Second attempt; the first failed independent
controller verification. The worktree for this attempt started clean at `38ca2f8`, with none of
the first attempt's work present, so this is an authoring pass rather than a repair of a diff I
could read.

### Intended outcome

Author the user-baseline migration — profiles, locations, equipment-per-location — with owner-only
RLS, the `weight_unit` profile default and typed absence, and prove it with
`supabase db push --dry-run` **without mutating the reused live project**. The off-machine-copy
checkbox in `docs/backend/live-inventory.md` is still unchecked, so the mutation gate is closed
and stays closed here.

### What changed locally

- **`supabase/migrations/20260921000001_user_baseline.sql`** — new. Enums (`weight_unit` new;
  `experience_level`, `goal_preset`, `equipment_tier` guarded so the reused project's live value
  sets carry forward), the drop of the two Replace tables, `profiles`, `locations`,
  `location_equipment`, the profile-on-signup trigger, and eleven owner-scoped RLS policies with
  matching grants.
- **`src/test/user-baseline-migration.test.ts`** — new. 22 tests against the shipped artefact,
  mirroring the catalog test's approach: comments stripped before assertions, string literals
  blanked again before any *absence* assertion, since `COMMENT ON` legitimately names the columns
  being retired.
- **`src/test/catalog-migration.test.ts`** — amended, necessarily. Two of its assertions enumerated
  the migrations directory exactly (`rebuildMigrations` equals `[CATALOG_MIGRATION]`, and
  `migrationFiles` equals the inherited list plus the catalog file). Both would have gone red on
  the mere existence of a second domain. They were rewritten to preserve their intent rather than
  relaxed: the first now asserts that exactly one migration in the rebuild series *defines a
  catalog table*, which is a stronger statement of "one migration per domain" than a filename
  equality; the second asserts the inherited series is exactly the 29 captured versions, that every
  file belongs to one series or the other, and that the catalog still leads the rebuild series.

Nothing external changed. No push, no PR, no live mutation.

### Decisions, and why

- **The replaced tables are dropped, not `IF NOT EXISTS`-ed around.** `profiles` and `locations`
  are Replace (`docs/backend/dispositions.md` §1) and already exist on the reused project in their
  previous shape. `create table if not exists` would be a silent no-op there: no `weight_unit`, no
  equipment table, and the previous RLS policies left standing — every acceptance criterion would
  read as satisfied by the file while being false in the database. That failure mode is quiet,
  which is what makes it worth the destructive statement. It is grounded in the owner decision that
  all personal rows are disposable, takes effect only after the recoverable snapshot
  (`docs/backend/snapshot/2026-09-18T162821Z`), and cannot run before TASK-072 clears the gate.
  On an empty project it finds nothing. A test asserts the drop precedes the create, and that the
  only tables dropped are the two this domain owns.
- **"Exactly one default location per user" is two constraints, because it is two claims.** At most
  one is a partial unique index on `(user_id) WHERE is_default` — enforced by the index itself,
  where the previous schema used a trigger that un-defaulted the other rows *after* the violation.
  Exactly one, where the user has any, is a `DEFERRABLE INITIALLY DEFERRED` constraint trigger
  checked at COMMIT, so moving the default between two locations is not a violation in between.
  Zero locations means no default, which is correct rather than a special case.
- **`profiles.default_location_id` is not rebuilt.** It duplicated `locations.is_default` and the
  two could disagree about which location was the default. The flag is the one a constraint can
  police, so the pointer went.
- **Equipment is rows, not `TEXT[]`.** One row per item per location, so DATA-05 can exclude a
  single piece of equipment and eligibility can join rather than unnest. `equipment_id` carries no
  FK because DATA-01a models equipment as text ids on `exercise_definitions.equipment_options` —
  there is no equipment table to point at. An unmatched id makes no exercise eligible, which is the
  same outcome as not owning it.
- **`location_equipment` has no `user_id`.** Its policies walk to `locations` instead. A
  denormalized owner column could disagree with the location the row hangs off, and the policy
  would then enforce the wrong answer confidently.
- **`profiles` has no DELETE policy and no DELETE grant.** Deleting the row would strand an
  authenticated user with no profile; account deletion removes the `auth.users` row and this one
  cascades. Stated as an absence in the test, not just in a comment.
- **Typed absence:** an onboarding answer not yet given is NULL (`experience_level`, `goal_preset`),
  and `onboarded_at` NULL means onboarding is incomplete. A failed profile *read* is an error state
  and never this NULL — that distinction is defect D1, and it is a guard-level rule.
- **`weight_unit` is a default for new writes only.** Nothing in this migration joins a set log to
  the profile to resolve a unit; DATA-01d stamps the unit per row. A test asserts this migration
  names no log table and defines no view, because either would be a route by which changing the
  default could reinterpret recorded history.

### Failures and corrections

- **`NEW` is unassigned in a DELETE trigger.** The first version of the default-location assertion
  read `coalesce(new.user_id, old.user_id)`, which raises at runtime on DELETE rather than
  returning NULL. Corrected to branch on `TG_OP`, and to re-check *both* owners on UPDATE so a row
  moved between users cannot leave the source user defaultless. The test asserts both `TG_OP`
  branches exist, so the mistake cannot return unnoticed.
- **One test failed on first run** — `enabled_sections`' default parsed as empty. The slice started
  at the column name, and `section_type[]` closes a bracket before the array literal begins. Fixed
  by slicing from `array[`.

### Verification, exact results

Run from the workspace on the final tree:

- `supabase db push --dry-run` — exit 0. `Would push these migrations: •
  20260921000000_catalog_domain.sql • 20260921000001_user_baseline.sql`, and
  `{"upToDate":false,"dryRun":true,…}`. Connected to the remote project; nothing applied.
- `npx vitest run` — 39 files, 402 tests, all passed. 22 of them are this task's new file; the
  other 380 are the suite as it stood, including the two amended catalog assertions.
- `npm test -- --run` — same, 402 passed.
- `npm run lint` — 0 errors, 42 warnings, all pre-existing `react-refresh/only-export-components`
  in `src/dev/gallery-registry.tsx` and untouched by this change.
- `npx tsc --noEmit` — clean.
- `npm run build` — clean, built in 121ms.

### What is proven, and what is not

The dry run proves the CLI accepts the migration set against the real project. The tests prove the
migration says what DATA-01b requires: RLS enabled on all three user tables, every one of the
eleven policies scoped `TO authenticated` with an owner predicate on **both** `USING` and
`WITH CHECK` where the command has both (`USING` alone on an UPDATE would let a user rewrite their
own row into someone else's ownership), no `USING (true)` anywhere, privileges revoked from `anon`
entirely, and grants issued only for commands that have a policy.

**The two-user behavioural proof REQ-010 asks for is not delivered here, and cannot be.** Proving
that user A cannot read user B's rows needs a database to prove it against: the gate forbids
touching the live one, and there is no local Postgres — only `libpq`'s client binaries are
installed, `supabase start` needs Docker, and ENV-04 keeps Docker out of the loop. That check is
already owned and named: **PC-2b** in `docs/backend/post-cutover-checks.md` ("User A cannot read
user B's rows on every owner-scoped table"), with ENV-07 making it continuous. No new checklist
entry was added, because the existing one already covers these tables.

The SQL was also not executed by a parser. An attempt to validate the grammar with `pglast` in a
throwaway venv outside the repository was refused by the command sandbox, so the DDL was reviewed
by hand against the Postgres grammar instead — in particular the `CREATE CONSTRAINT TRIGGER`
clause order, where `DEFERRABLE INITIALLY DEFERRED` must precede `FOR EACH ROW`.

### Unresolved / next safe action

- **The off-machine dump copy remains the blocking precondition.** Until
  `docs/backend/live-inventory.md` records it, no DATA task may apply anything — including this
  migration's drops, which are the first genuinely destructive statements the rebuild has authored.
- **`goal_preset` diverges by project.** A fresh database gets the five reviewed values; the reused
  project keeps its live set, which still contains the legacy `quick` that migration `00020`
  removed from the UI but left in the type. The guard is what carries the Transform disposition
  forward, so this is deliberate — but it means the two databases are not value-identical until
  someone drops `quick`, and no task currently owns that.
- **`PROJECT_MAP.md` was not updated.** `supabase/migrations/` is already described there as "one
  migration per domain"; a second domain is the architecture working as documented, not a change
  to it.
