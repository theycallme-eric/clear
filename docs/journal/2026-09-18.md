# 2026-09-18 — TASK-008 backend audit gate (issue #80, attempt 2)

## Intent

Implement REQ-008 on branch `agent-runner/task-008-a2-a41bb625`: live inventory, recoverable
schema/catalog snapshot, live diff, per-surface dispositions, rollback procedure, post-cutover
checks, and reconciliation of repository docs around the reused-project/new-platform boundary.
Attempt 1 (`agent-runner/task-008-a1-a41bb625`) left no commits; this attempt starts from
`origin/main` (`5b50a55`).

## Blocking finding: live capture impossible in this execution environment

Every command touching the network, environment variables, or non-allowlisted executables was
denied by the runner's command-approval layer with no interactive approver present. Denied (each
tried once, then adapted): `gh issue view`, `npx supabase projects list`, `psql`, `curl` to the
project health endpoint, `printenv`/`env`/`python3`/`node`/`sh` presence checks, and custom
`npm run` script names. Allowed: git, file reads/writes, `cp` with exact paths, and the exact
controller commands (`npm run lint`, `npx tsc --noEmit`, `npm test -- --run`, `npm run build`).

Consequence: PRE-003/PRE-004/PRE-007 could not even be *verified present*, and the read-only live
inventory and snapshot could not be executed here. This mirrors the prior audit's experience
(`source-02`: live state "remains a named gate, not an inferred fact"). Rather than fake a
capture, the deliverables make the capture rehearsable and mark it as the explicit pending gate.

## What was produced

- `docs/backend/live-inventory.md` — full expected-state inventory derived from the 31 evidence
  migrations, functions, and config (tables, views, RPCs, triggers, policies, enums with values,
  Edge Functions, auth config, env dependencies); rebuild-model diff frame; the 140-vs-173
  question; §5 "Live capture — PENDING" checklist that is the mutation gate.
- `docs/backend/dispositions.md` — preserve/transform/replace/retire for every surface class,
  grounded in the 2026-09-07 owner decisions; approval record block (status PROPOSED) naming how
  Eric records approval on issue #80.
- `docs/backend/rollback.md` — rehearsal step plus live restore procedure from the full dump.
- `docs/backend/post-cutover-checks.md` — named checks PC-1a…PC-6c.
- `docs/backend/evidence/` — read-only vendored copies of the previous app's 31 migrations, both
  Edge Function sources, and local Supabase config (diff baseline committed offline).
- `scripts/backend-audit/` — `check-prereqs.mjs`, `inventory.sql` (wrapped in a READ ONLY
  transaction by) `inventory.sh`, and `snapshot.sh`; npm scripts `backend:prereqs`,
  `backend:inventory`, `backend:snapshot`. `backups/` gitignored so full dumps with personal test
  rows can never be committed.
- Reconciled operative docs to "reused Supabase project + new CLEAR platform":
  `docs/process/IMPLEMENTATION_PLAN.md` (three passages), `docs/process/HUMAN_GATES.md`
  (reworded two gates, added the REQ-008 approval gate), `docs/process/CATALOG_MIGRATION_SCOPE.md`
  (same-project clarification), `PROJECT_MAP.md` (two new boundary rows). Frozen
  `docs/requirements/REQUIREMENTS.md` and `docs/_archive/` deliberately untouched.
- `eslint.config.js`: added `docs/backend/evidence` to ignores (vendored Deno sources), matching
  the existing `docs/design/exports` precedent.

## Verification

Controller-required commands, run from the workspace on the final tree — results recorded in the
handoff message: `test -s docs/backend/live-inventory.md`, `test -s docs/backend/dispositions.md`,
`npm run lint`, `npx tsc --noEmit`, `npm test -- --run`, `npm run build`.

## Unresolved at the runner handoff

1. **Owner approval is not obtained** — the dispositions are PROPOSED. Next safe action: Eric (or
   a session with command approval) runs `npm run backend:prereqs`, `backend:inventory`,
   `backend:snapshot`, fills `live-inventory.md` §5, adjusts dispositions for any deviation, and
   records approval on issue #80.
2. Issue #80's comments could not be read (`gh` denied); if they contain later owner decisions,
   they must be reconciled before approval.
3. The 140-vs-173 exercise count remained open until the owner-supervised continuation below.
4. No live mutation occurred in this session; nothing was pushed, and no PR was opened, per the
   execution contract.

## Owner-supervised continuation after the runner handoff

The owner supplied PRE-003/PRE-004 and reauthenticated PRE-007 outside the headless worker. The
operator then ran the generated tools under explicit read-only approval. The first inventory run
failed safely because it referenced `component_movements`, a column absent from the live project;
the open transaction rolled back automatically. The query was corrected to inventory optional
columns without referencing them, then rerun successfully.

Captured facts:

- live migration history ends at `00029`; `00030` and `00031` are absent;
- 140 exercise definitions, 150 anchor links, 488 muscle mappings, and 27 legacy movement-pattern
  rows are live; there is no 173-row live catalog;
- eight disposable test auth users exist;
- only `generate-workout` is deployed, and legacy-JWT verification is off;
- the old Vercel site/reset URL remains in Auth, email is the only enabled provider, OTP is 8 digits
  with a 3600-second expiry, and custom SMTP is disabled.

The schema/catalog snapshot lives under `docs/backend/snapshot/2026-09-18T162821Z/`. A verified
custom-format full dump was copied outside the temporary worktree and remains excluded from Git.
No live mutation occurred. Remaining human gates are owner approval of the updated dispositions and
an off-machine copy of the full dump before any mutation.

Workflow friction recorded for requirements/runner review:

1. TASK-008 required live read-only commands, but the approved task exposed only file-existence
   verification commands to the worker allowlist.
2. PRE-004 did not declare a PostgreSQL client-tool prerequisite; client-only `libpq` had to be
   installed before the audit could run.
3. The generated SQL assumed the newest evidence schema and initially failed on an older live
   schema; audit queries must feature-detect optional columns.
4. The Supabase CLI management call stalled after SQL capture; authenticated dashboard inspection
   supplied the function, secret-name, and auth-configuration evidence without exposing values.

## Owner approval

Eric approved the live-captured dispositions on 2026-09-18. The approval is recorded on issue #80
against capture commit `39fe013`. This approves the 140-row live catalog as canonical and the
reused-project/new-platform boundary. Live mutation remains blocked until the verified full dump
has a second off-machine copy.
