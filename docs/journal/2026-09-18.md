# 2026-09-18 — ENV-06 test harness (TASK-006, issue #6)

## Intended outcome

Turn the ENV-01 Vitest install into a usable harness: shared render helper, domain fixture
factories, an example component test that queries by accessible role, coverage reported but not
gated, and the `vitest --watch` working loop documented in DEVELOPMENT.md. Worked in the isolated
Agent Runner worktree on branch `agent-runner/task-006-a1-506dc3d9`; no push or PR (runner
contract).

## What changed

- `src/test/render.tsx` — new. `AppProviders` is the single place app-level providers mount;
  `renderWithProviders(ui)` wraps a component in providers plus a `MemoryRouter`;
  `renderApp(entries)` mounts the real route tree via `createTestRouter`.
- `src/test/factories.ts` — new. Domain fixture factories; currently `makeAppError` built on
  `createError` from `src/state/errors.ts` with partial overrides.
- `src/test/example.test.tsx` — new. The ENV-06 example component test: renders the design-system
  `Checkbox` (public entry) through the helper, queries `getByRole('checkbox', { name })`, asserts
  checked state and visible label; second case proves fixtures come from the factory.
- `src/app/router.test.tsx` — refactored to use `renderApp` instead of hand-mounting
  `RouterProvider`, per the "utilities live in one place" criterion.
- `vite.config.ts` — coverage config (v8 provider, text/html/lcov reporters, no thresholds;
  excludes the vendored `src/design-system/**`, test utilities, and `main.tsx`).
- `package.json` — added `test:coverage` script (`vitest run --coverage`).
- `DEVELOPMENT.md` — new, scoped to the harness: watch loop, test-writing rules, coverage. ENV-04
  will add the environment-setup content later; the header says so to avoid a scope collision.

## Failures and blocked items

- `npm install --save-dev --save-exact @testing-library/user-event @vitest/coverage-v8` was
  denied by the sandbox in every form tried (normal, `--offline`, unsandboxed). Neither package is
  in the local npm cache or `node_modules`. Consequence: two ENV-06 acceptance boxes cannot be
  fully closed in this session:
  - *user-event as the interaction API* — declared as policy in DEVELOPMENT.md; the example test
    asserts rendered accessible state without an interaction. No `fireEvent` anywhere.
  - *coverage reported* — config and script are in place and correct, but `npm run test:coverage`
    will prompt for `@vitest/coverage-v8` until someone with network runs the install.
  Next safe action: run the exact install command above (it also updates `package-lock.json`),
  then `npm run test:coverage` to confirm the report prints.
- Deliberately did not add either package to `package.json` without a lockfile update, because
  `npm ci` in CI fails on a package.json/lock mismatch.

## Verification

All controller-required commands run from the worktree after the changes:

- `npx vitest run` — 4 files, 33 tests, all passed (includes the new example test).
- `npm run lint` — first run emitted one `react-refresh/only-export-components` warning for
  `src/test/render.tsx` (exit 0). Fast refresh does not apply to test utilities, so the rule is
  now off for `src/test/**` and `src/**/*.test.{ts,tsx}` in `eslint.config.js`; re-run clean,
  zero warnings.
- `npx tsc --noEmit` — clean.
- `npm test -- --run` — 4 files, 33 tests, all passed.
- `npm run build` — tsc + vite build succeeded, 31 modules transformed.

## Unresolved

- Install of `@testing-library/user-event` and `@vitest/coverage-v8` (blocked, see above).
- CI already runs `npm test`; once the coverage provider is installed, deciding whether CI should
  run `test:coverage` for the "on a wall" visibility is a one-line follow-up, not done here.
- `git fetch` is network-blocked in this sandbox. The locally known `origin/main` (5b50a55) is
  already an ancestor of HEAD; the controller must re-verify against the live remote.
