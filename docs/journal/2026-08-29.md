# 2026-08-29 — GitHub migration baseline

- Verified that prior local issue numbers were not proof of a GitHub migration; the target repo did
  not exist.
- Created private `theycallme-eric/clear` and pushed the full 23-commit planning history to `main`.
- Imported the approved `clear-design-system@0.5.0` artifact that the specs referenced but the repo
  did not contain.
- Closed the four pre-issue readiness gaps: visual mappings, motion choreography, composition prompt,
  and an end-to-end generation/reconstruction example.
- Found and fixed an issue-generator boundary bug that appended requirements-document sections 13–14
  to the final issue body.
- Promoted the frozen issue baseline to requirements v0.7. The next irreversible handoff is creating
  the 71 GitHub issues and their 136 dependency relationships.
- Began the sole ready node, ENV-01, while GitHub CLI authorization waited on an email verification:
  moved the planning control plane under `docs/`, added the React 19/Vite/strict-TypeScript shell,
  tested root and fallback routing, documented project boundaries, and verified build/lint/type/test.

## GitHub migration completion

### Intended outcome

Make the requirements navigable and operational in GitHub: labels and milestones, one issue per
v0.7 requirement, native blocked-by relationships for the DAG, and an ENV-01 pull request tied to
the first ready issue.

### Authentication and repository state

- Ordinary Git HTTPS access was already working and had pushed `main` plus the
  `env-01-scaffold` branch. GitHub CLI authentication is separate because issue, milestone,
  dependency, and pull-request operations use the GitHub API.
- The first browser device authorization showed success, but `gh auth status` still reported the
  previously stored token as invalid. The success screen alone was therefore not treated as proof.
- Removed only the stale local GitHub CLI credential, repeated the official GitHub CLI device flow,
  and verified the result from the CLI: active account `theycallme-eric`, HTTPS Git protocol, token
  stored in the macOS keyring, private-repository scope available. No token or one-time code is
  recorded here.

### Migration failures and corrections

1. `02-create-issues.sh` stopped before creating an issue with `REPO…: unbound variable`.
   Bash read the adjacent Unicode ellipsis as part of the variable name. The generator now emits
   `${REPO}…`; the same correction was applied to the dependency script template.
2. Issue creation then stopped safely after issue 66. Requirement OVR-01c contains quoted text in
   its title (`why this number`), and the generator had embedded titles directly inside shell double
   quotes. The generator now shell-quotes every generated argument with Python `shlex.quote`.
   The local ignored `issue-map.txt` checkpoint let the rerun skip issues 1–66 and create only
   issues 67–71.
3. The first live verification incorrectly reported 142 edges and no ready issue. GitHub returns
   `blockedBy` as an object with `nodes` and `totalCount`; measuring the object's length returns two
   for every issue. The corrected verification sums `blockedBy.totalCount` and tests that field for
   zero. No graph data was removed because the mismatch was in the query, not GitHub.

### Verified result

- Private repository: `theycallme-eric/clear`.
- Branches: `main` and `env-01-scaffold`.
- Four milestones and the generated label set are live.
- All 71 requirement issues are live.
- All 136 native blocked-by relationships are live.
- The only open issue with zero blockers is issue 1, ENV-01, matching the frozen DAG.

### Synchronization model

- The Mac is the working copy. A commit is a local checkpoint; a push sends that branch's commits
  to GitHub. GitHub does not continuously synchronize uncommitted files.
- `main` is approved work. One issue is implemented on one branch and merged through one pull
  request. After a merge, local `main` is updated with a fast-forward pull before new work starts.
- GitHub Issues are now the live build queue. `docs/requirements/REQUIREMENTS.md` remains the frozen
  v0.7 baseline; discovered scope changes belong on the live issue or a deliberate follow-up issue.
- `docs/github/issue-map.txt` is a local, ignored migration checkpoint. It maps requirement IDs to
  issue numbers and is not product source code.

### Application reality at this checkpoint

- Runtime code exists, but only at scaffold depth: React mounts, browser routing works, `/` renders
  a minimal CLEAR shell, unknown routes render a tested fallback, and build/lint/type/test tooling
  passes.
- The design-system export is a self-contained package with CSS, JavaScript/JSX components, type
  declarations, icons, specimens, and three `.dc.html` composition templates. The `.dc.html` files
  are design templates, not the whole UI library and not runtime routes.
- The rebuild contains no Supabase client, migrations, edge functions, environment contract, or
  live backend integration yet. Backend behaviour is specified in the data and generation specs and
  scheduled in the DATA, AUTH, CORE, and GEN issue chains; historical UI files in the design export
  are reference material, not a current backend.

### Next safe action

Pull request 72 (`[ENV-01] Repository scaffold`) is open from `env-01-scaffold` into `main`, links
issue 1 with `Closes #1`, and GitHub reports a clean merge state. GitHub reports no automated PR
checks because the CI pipeline is intentionally ENV-02, the next layer of work; the local lint,
test, build, generator, DAG, and live-graph checks are recorded in the PR body. Review and merge
ENV-01; closing issue 1 will release the first real implementation wave in the GitHub DAG.

### Cross-session journal persistence

Added `.cursor/rules/process-journal.mdc` as an always-applied project rule. Future Codex/Cursor
sessions are instructed directly to maintain the dated journal at material checkpoints and before
handoff, including failures, causes, corrections, verification, and unresolved questions. The rule
excludes secrets and question-only orientation sessions. Claude sessions continue to receive the
same convention through `CLAUDE.md`, which requires reading the agent playbook and its §10 journal
policy.

Anthropic persistence was then made explicit rather than indirect. Added the unconditional Claude
Code rule `.claude/rules/process-journal.md` and a concise `Process journal` section in the root
`CLAUDE.md`. Current Claude Code documentation says the root project `CLAUDE.md` is loaded at every
session start and unscoped Markdown files under `.claude/rules/` are loaded unconditionally. The
Cursor/Codex rule remains as a compatible second-tool entry point; both state the same policy.

Codex persistence was made canonical as well. Added a root `AGENTS.md`, which Codex discovers at
project startup, with the ready-queue constraint and the same process-journal requirements. The
existing `.cursor/rules/process-journal.mdc` remains in place for compatible clients. CLEAR can now
be worked with Anthropic tooling or Codex without relying on either tool to understand the other
vendor's instruction-file convention.

## Post-merge runway and catalog scope

### Intended outcome

After PR 72 merged, synchronize local `main`, preserve all reusable old exercise-catalog data while
excluding user history, and make the live DAG directly operable by Anthropic tooling and Codex for
longer unattended runs.

### Repository and GitHub changes

- Fast-forwarded local `main` to merged commit `35b938e`; the working copy matched `origin/main`
  before new work began.
- Created live follow-up issue 73, OPS-01, for the automation itself so process work does not widen a
  product requirement. Connected it to closed ENV-01 and created branch `ops-01-dag-runner`.
- Recorded Eric's catalog-only migration decision on DATA-02: keep all reusable exercise/reference
  metadata; exclude auth users, profiles, locations, workouts, sessions, logs, and other history.

### Catalog audit and corrected assumption

- Audited the archived private repository `theycallme-eric/clear-app` without reading or copying any
  secret values or user data.
- The committed migrations insert 145 unique exercise IDs, then consolidate/remove variants to 140
  final definitions. The final tag migration covers all 140; the muscle junction seed contains 488
  rows; 13 secondary anchor mappings are explicit.
- DATA-02 expects 173 exercises. The old repository is therefore a reproducible partial source, not
  proof of live-database equivalence. The 33-row difference must be reconciled against the old live
  Supabase catalog. The expected count was not silently changed.

### Automation correction and implementation

- A prior readiness check used `blockedBy.totalCount`; GitHub includes closed blockers in that total,
  which makes newly released work appear blocked. The runner now cross-checks GitHub's native
  `is:open -is:blocked` search and independently counts only dependency nodes whose state is OPEN.
- Added a deterministic ready-queue command that ranks available work by milestone, open fan-out,
  then critical-path membership and marks issues already attached to open PRs as in progress.
- Added one canonical runner policy, a known-human-gates map, the catalog migration boundary, and
  thin project skills for Claude Code and Codex. Runway mode may continue to another independent
  ready issue when one issue reaches a gate; it does not auto-merge or bypass credentials.

### Verification and next safe action

- The live runner cross-check passed: seven ready issues (the six released product nodes plus
  OPS-01), all available, with DS-01 recommended first by its 10 open dependents.
- Runner unit tests: 4 passed. Frozen graph validation: 71 requirements, 136 edges. Application
  verification: ESLint passed; Vitest 1 file/2 tests passed; TypeScript and Vite production build
  passed with 25 modules transformed. `git diff --check` passed.
- Committed and pushed branch `ops-01-dag-runner`; opened mergeable PR 74 with `Closes #73`. GitHub
  reports no automated checks because ENV-02 is still an available, unimplemented node.
- Re-ran the live command after opening the PR: seven issues remained dependency-ready, OPS-01 was
  correctly marked `PR #74`, six remained available, and DS-01 remained the recommendation. This
  verifies duplicate-work suppression against a real closing-issue reference.
- Next safe action: review and merge PR 74. Then synchronize `main`; invoking the
  `clear-dag-runner` skill in runway mode can begin the available product queue. Service-specific
  issues pause only at the documented human gates.
