# Taxonomy equivalence — DATA-02

GENERATED by `npm run seed`. Do not hand-edit; `npm run seed -- --check`
fails if this file has drifted from the transform.

Capture: `docs/backend/snapshot/2026-09-18T162821Z/catalog/` — the read-only live
read of the reused project, 2026-09-18.  
Reviewed tags: `docs/backend/evidence/previous-migrations/00031_tag_exercises.sql`
— authored and reviewed, never deployed (live migration history ends at
`00029`), so the capture has no `component_movements` column to export and
this is the only place those tags exist.

Nothing here was applied to a database. Live application is TASK-072, behind
the off-machine-backup gate in `docs/backend/live-inventory.md`.

## 1. Preservation set

| Row | Captured | Seeded | Destination |
|---|---:|---:|---|
| Exercise definitions | 140 | 140 | `exercise_definitions` |
| Exercise-muscle mappings | 488 | 488 | `exercise_muscle_groups` |
| Exercise-anchor links | 150 | 102 | `exercise_pattern_weights` |
| Legacy movement patterns | 27 | — | derived from `component_movements` |
| Reviewed anatomy tags | 140 | 140 | two columns of `exercise_definitions` |

The 173-row expectation is not reachable from this input and is not
regenerated; the audited authority is 140.

## 2. What became of the 150 anchor links

| Disposition | Links | Why |
|---|---:|---|
| `weighted` | 102 | the anchor names a `movement_pattern`; the authored ranking is preserved |
| `dropped_non_pattern` | 48 | `surprise` — the old enum's name for core, conditioning and mobility work |
| `dropped_region` | 0 | a `session_focus`, not a pattern; the capture contains none |

Of the 102 preserved weights, 92 are primary and
10 secondary — the ranking `exercise_pattern_weights` exists to keep.

The dropped `surprise` links are the only content leaving the taxonomy, and
the seed proves the drop is lossless: every exercise whose anchors were all
`surprise` is still reachable through its preserved `exercise_role` or its
preserved `sections`. That check fails the build if it ever stops holding.

## 3. Weights the derivation does not reach

19 preserved weights rank a pattern `exercise_patterns` does not derive
from the exercise's components, so `exercise_pattern_ranked` cannot surface
them. They are seeded rather than discarded — the approved disposition is
Transform, and re-tagging an exercise inside a seed would be inventing
taxonomy. This is DATA_MODEL §3 step 4, "any lost primary/secondary
distinctions", enumerated.

| Exercise | Weight | Components | Derived |
|---|---|---|---|
| `bb-hip-thrust` | hinge (primary) | posterior-chain-activation | — |
| `bicep-curls` | pull (primary) | grip | — |
| `curl-to-press` | pull (primary) | vertical-press, grip | press |
| `deadlift` | pull | hip-hinge, posterior-chain-activation, grip, brace | hinge |
| `external-rotation` | pull (primary) | shoulder-mobility | — |
| `face-pulls` | pull (primary) | scapular-control, shoulder-mobility | — |
| `hammer-curls` | pull (primary) | grip | — |
| `high-pulls` | pull (primary) | triple-extension, scapular-control | power |
| `hip-thrust` | hinge (primary) | posterior-chain-activation | — |
| `internal-rotation` | pull (primary) | shoulder-mobility | — |
| `leg-curl` | hinge (primary) | posterior-chain-activation | — |
| `nordic-curl` | hinge (primary) | posterior-chain-activation | — |
| `preacher-curls` | pull (primary) | grip | — |
| `rear-delt-flys` | pull (primary) | scapular-control, shoulder-mobility | — |
| `shrugs` | pull (primary) | grip | — |
| `single-leg-glute-bridge` | hinge (primary) | posterior-chain-activation, single-leg-stability | unilateral |
| `single-leg-rdl` | pull | hip-hinge, posterior-chain-activation, single-leg-stability | hinge, unilateral |
| `snatch-grip-deadlift` | power (primary) | hip-hinge, posterior-chain-activation, grip, brace | hinge |
| `sotts-press` | power (primary) | vertical-press, knee-flexion, brace | press, squat |

## 4. Candidate sets, derived versus original

DATA_MODEL §3 step 3, across all four focuses. Both columns apply the
previous function's role exemption, so the delta is the taxonomy change and
nothing else. *With contrast* is the set the previous Edge Function actually
prompted with, including its `CONTRASTING_ANCHORS` widening; that heuristic
is not what `focus_pattern_map` replaces, so it is context rather than the
comparison basis.

| Focus | Patterns | Old anchors | Old | With contrast | New | Lost | Gained |
|---|---|---|---:|---:|---:|---:|---:|
| `upper_body` | press, pull | press, pull | 87 | 87 | 79 | 11 | 3 |
| `lower_body` | hinge, squat, unilateral | squat, hinge | 75 | 118 | 82 | 4 | 11 |
| `full_body` | hinge, press, pull, squat, unilateral | squat, hinge, press, pull | 118 | 118 | 115 | 13 | 10 |
| `power` | power | power | 54 | 54 | 54 | 2 | 2 |

**`upper_body`**  
Lost: `bicep-curls`, `deadlift`, `external-rotation`, `face-pulls`, `hammer-curls`, `high-pulls`, `internal-rotation`, `preacher-curls`, `rear-delt-flys`, `shrugs`, `single-leg-rdl`  
Gained: `push-jerk`, `sotts-press`, `split-jerk`

**`lower_body`**  
Lost: `bb-hip-thrust`, `hip-thrust`, `leg-curl`, `nordic-curl`  
Gained: `barbell-row`, `clean-pull`, `hang-clean`, `hang-snatch`, `pendlay-row`, `power-clean`, `snatch`, `snatch-grip-deadlift`, `sotts-press`, `split-jerk`, `squat-clean`

**`full_body`**  
Lost: `bb-hip-thrust`, `bicep-curls`, `external-rotation`, `face-pulls`, `hammer-curls`, `high-pulls`, `hip-thrust`, `internal-rotation`, `leg-curl`, `nordic-curl`, `preacher-curls`, `rear-delt-flys`, `shrugs`  
Gained: `clean-pull`, `hang-clean`, `hang-snatch`, `power-clean`, `push-jerk`, `snatch`, `snatch-grip-deadlift`, `sotts-press`, `split-jerk`, `squat-clean`

**`power`**  
Lost: `snatch-grip-deadlift`, `sotts-press`  
Gained: `high-pulls`, `swing`

Every *lost* entry appears in §3 — an exercise leaves a candidate set exactly
when its old anchor outlived its components. Every *gained* entry is an
exercise whose tags carry a pattern the single old anchor had no room to
state: the Olympic lifts and rows gain `lower_body` through `hip-hinge`, the
jerks gain `upper_body` through `vertical-press`, `high-pulls` and `swing`
gain `power` through `triple-extension`. That asymmetry is the change working.

## 5. The 27 legacy movement patterns

Not rebuilt. Patterns derive from `component_movements`, so there is no table
to keep in sync. Each row still has to be shown to carry nothing the new
model cannot say: its `anchor` is the primary anchor of every exercise that
pointed at it (and so is now a weight), and its `category` is a
`session_focus` name — except `core`, which is a section.

| Pattern | Exercises | Anchor → | Category → |
|---|---:|---|---|
| `conditioning-carry` | 4 | surprise → `exercise_role` + `sections` | full_body → `full_body` focus |
| `conditioning-locomotion` | 7 | surprise → `exercise_role` + `sections` | full_body → `full_body` focus |
| `conditioning-plyometric` | 5 | surprise → `exercise_role` + `sections` | full_body → `full_body` focus |
| `core-anti-rotation` | 4 | surprise → `exercise_role` + `sections` | core → `core` section |
| `core-flexion` | 6 | surprise → `exercise_role` + `sections` | core → `core` section |
| `core-rotation` | 2 | surprise → `exercise_role` + `sections` | core → `core` section |
| `core-stability` | 7 | surprise → `exercise_role` + `sections` | core → `core` section |
| `hinge-deadlift` | 8 | hinge → `exercise_pattern_weights` | lower_body → `lower_body` focus |
| `hinge-extension` | 2 | hinge → `exercise_pattern_weights` | lower_body → `lower_body` focus |
| `hinge-hip-thrust` | 5 | hinge → `exercise_pattern_weights` | lower_body → `lower_body` focus |
| `hinge-swing` | 1 | hinge → `exercise_pattern_weights` | lower_body → `lower_body` focus |
| `mobility-stretch` | 13 | surprise → `exercise_role` + `sections` | full_body → `full_body` focus |
| `power-clean` | 5 | power → `exercise_pattern_weights` | full_body → `full_body` focus |
| `power-jerk` | 2 | power → `exercise_pattern_weights` | full_body → `full_body` focus |
| `power-snatch` | 3 | power → `exercise_pattern_weights` | full_body → `full_body` focus |
| `power-thruster` | 3 | power → `exercise_pattern_weights` | full_body → `full_body` focus |
| `press-accessory` | 4 | press → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `press-horizontal` | 11 | press → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `press-isolation` | 2 | press → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `press-vertical` | 5 | press → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `pull-accessory` | 9 | pull → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `pull-horizontal` | 6 | pull → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `pull-isolation` | 0 | pull → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `pull-vertical` | 6 | pull → `exercise_pattern_weights` | upper_body → `upper_body` focus |
| `squat-bilateral` | 9 | squat → `exercise_pattern_weights` | lower_body → `lower_body` focus |
| `squat-machine` | 2 | squat → `exercise_pattern_weights` | lower_body → `lower_body` focus |
| `squat-unilateral` | 9 | squat → `exercise_pattern_weights` | lower_body → `lower_body` focus |

Unreferenced: `pull-isolation` — named rather than silently absent.

## 6. Reviewed workout-anatomy tags

All 140 exercises carry a reviewed `exercise_role` and a component
vocabulary. The frequencies below are the independent check on migration
`00031`: `DATA_MODEL.md` §3 lists the same counts, written from the live
catalog before this seed existed, and they agree exactly.

| Component | Tagged | Pattern |
|---|---:|---|
| `brace` | 61 | — (a demand, not a pattern) |
| `knee-flexion` | 27 | squat |
| `scapular-control` | 25 | — (a demand, not a pattern) |
| `posterior-chain-activation` | 25 | — (a demand, not a pattern) |
| `grip` | 25 | — (a demand, not a pattern) |
| `hip-mobility` | 22 | — (a demand, not a pattern) |
| `hip-hinge` | 20 | hinge |
| `vertical-press` | 18 | press |
| `triple-extension` | 17 | power |
| `shoulder-mobility` | 14 | — (a demand, not a pattern) |
| `horizontal-press` | 13 | press |
| `cardio-output` | 12 | conditioning |
| `single-leg-stability` | 11 | unilateral |
| `ankle-mobility` | 8 | — (a demand, not a pattern) |
| `horizontal-pull` | 7 | pull |
| `anti-rotation` | 7 | — (a demand, not a pattern) |
| `vertical-pull` | 5 | pull |
| `landing-mechanics` | 3 | — (a demand, not a pattern) |
| `thoracic-mobility` | 3 | — (a demand, not a pattern) |
| `anti-lateral-flexion` | 2 | — (a demand, not a pattern) |

| Role | Exercises |
|---|---:|
| `accessory` | 70 |
| `compound_lift` | 30 |
| `mobility` | 11 |
| `activation` | 10 |
| `conditioning` | 9 |
| `stability` | 7 |
| `cardio` | 3 |

## 7. Checks

48 checks, 0 failing.

### Counts

- PASS — exercise definitions: expected 140, seeded 140
- PASS — exercise-anchor links read: expected 150, read 150
- PASS — exercise-muscle mappings: expected 488, seeded 488
- PASS — legacy movement patterns: expected 27, read 27
- PASS — reviewed workout-anatomy tags: expected 140, read 140
- PASS — retired 173-row expectation not regenerated: no count equals the retired 173

### Duplicated

- PASS — exercise ids unique: 140 exercise ids, no repeats
- PASS — muscle mappings unique: 488 muscle mappings, no repeats
- PASS — pattern weights unique: 102 pattern weights, no repeats
- PASS — anchor links unique: 150 anchor links, no repeats
- PASS — at most one primary pattern per exercise: 92 primary pattern weights, no repeats

### Orphaned

- PASS — muscle mappings resolve: all 488 references resolve
- PASS — pattern weights resolve: all 102 references resolve
- PASS — anchor links resolve: all 150 references resolve
- PASS — regression/progression targets resolve: 16 exercises link to a variant, all resolving
- PASS — absent variants stay null: no empty-string variant survived the capture read
- PASS — default equipment is an option: every default_equipment appears in its equipment_options
- PASS — equipment display names resolve: every display-name key is one of that exercise’s equipment options
- PASS — legacy pattern references resolve: all 140 references resolve

### Dropped

- PASS — every captured exercise is seeded: all 140 captured ids present
- PASS — every captured muscle mapping is seeded: 488 muscle mappings, exactly
- PASS — every anchor link has a reviewed disposition: expected weighted=102 dropped_region=0 dropped_non_pattern=48; found dropped_non_pattern=48 weighted=102
- PASS — dropped non-pattern anchors are carried by role or section: all 45 "surprise"-only exercises remain reachable by exercise_role or sections
- PASS — unreferenced legacy patterns are the reviewed ones: expected [pull-isolation], found [pull-isolation]
- PASS — legacy pattern anchor survives as the primary link: every exercise’s primary anchor equals its legacy pattern’s anchor
- PASS — legacy pattern categories map to a session focus: without a session_focus counterpart: expected [core], found [core]

### Invented

- PASS — every pattern weight traces to one anchor link: 102 pattern weights, exactly
- PASS — authored ranking preserved verbatim: expected primary=92 secondary=10; found primary=92 secondary=10
- PASS — no exercise exists that the capture does not: 140 exercises, exactly
- PASS — exercise roles are enum values: every exercise_role is one of 7 allowed values
- PASS — sections are enum values: every section_type is one of 10 allowed values
- PASS — muscle roles are enum values: every muscle_role is one of 3 allowed values
- PASS — weight patterns are enum values: every movement_pattern is one of 7 allowed values
- PASS — only shared anchor names become patterns: every weighted anchor is one of 5 allowed values

### Workout-anatomy tags

- PASS — tags and definitions are one-to-one: 140 tagged exercises, exactly
- PASS — component vocabulary and frequency: 20 values, every count as reviewed
- PASS — exercise role distribution: 7 values, every count as reviewed
- PASS — muscle role distribution: 3 values, every count as reviewed
- PASS — muscle group vocabulary: expected 18 distinct muscle groups, found 18
- PASS — every exercise has muscle coverage: all 140 exercises carry at least one muscle mapping
- PASS — no component outside the reviewed vocabulary: 20 components, all reviewed
- PASS — every mapped component is actually used: all 9 components in component_pattern_map appear on a tagged exercise

### Candidate-set equivalence

- PASS — candidate set — upper_body: legacy 87 (87), derived 79 (79); lost 11 (11), gained 3 (3)
- PASS — candidate set — lower_body: legacy 75 (75), derived 82 (82); lost 4 (4), gained 11 (11)
- PASS — candidate set — full_body: legacy 118 (118), derived 115 (115); lost 13 (13), gained 10 (10)
- PASS — candidate set — power: legacy 54 (54), derived 54 (54); lost 2 (2), gained 2 (2)
- PASS — every session focus compared: expected 4 focuses (upper_body, lower_body, full_body, power), compared 4
- PASS — weights outside the derivation are the reviewed ones: 19 preserved weights that exercise_pattern_ranked cannot surface, all reviewed
