node_modules/
dist/
coverage/
# ENV-07 E2E artefacts: the trace and screenshot a failed run keeps, and the
# HTML report built from them. Diagnostics for the run that produced them —
# never source, and a trace can contain a session token.
test-results/
playwright-report/
blob-report/
playwright/.cache/
.env
.env.*
!.env.example
*.local
.DS_Store
# Full live-database dumps may contain personal test rows — never committed (REQ-008).
backups/
# Supabase CLI scratch: the linked project ref, branch state, and local stack
# artefacts. `supabase/config.toml` and `supabase/migrations/` are committed.
supabase/.temp/
supabase/.branches/
supabase/logs/
